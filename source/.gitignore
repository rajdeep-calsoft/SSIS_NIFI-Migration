# Runtime data (regenerated by the stack)
data/landing/*
data/reports/*
!data/**/.gitkeep

# ssis_sim/dtsx_builder.py regenerates these cosmetic package files as a
# side effect of running orchestrate.py -- they were investigated and found
# non-conformant (unusable as converter input, see docs/1.manager-demo.md),
# removed on purpose, and shouldn't come back just because setup ran again.
# pkg_orders_etl.dtsx is real and tracked; these six are not.
ssis_packages/pkg_cleanse_validate.dtsx
ssis_packages/pkg_dim_load.dtsx
ssis_packages/pkg_etl_control.dtsx
ssis_packages/pkg_fact_load.dtsx
ssis_packages/pkg_ingest_stage.dtsx
ssis_packages/pkg_reconcile.dtsx

# Local env
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
