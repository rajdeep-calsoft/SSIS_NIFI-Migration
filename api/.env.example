# Must match destination/.env's POSTGRES_PASSWORD -- the generated flow
# deploys into that same warehouse, using the SAME password, just injected
# under the name the orders_etl binding's password_ref asks for.
ORDERS_ETL_DB_MAIN_PASSWORD=etlpass
